precision mediump float; // precision for float computation

// this is the fragment (or pixel) x-shader
void main(void) {
    // Setting every pixel to white 
    // with alpha-channel value of 1.0
    gl_FragColor = vec4(1.0, 1.0, 1.0, 1.0);
}